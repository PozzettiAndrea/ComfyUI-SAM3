[project]
name = "sam3"
description = "ComfyUI integration for Meta's SAM3 model enabling open-vocabulary image segmentation using natural language text prompts, with automatic model download, geometric refinement, and flexible confidence thresholds."
version = "1.0.0"
license = {file = "LICENSE"} 
# classifiers = [
#     # For OS-independent nodes (works on all operating systems)
#     "Operating System :: OS Independent",
#
#     # OR for OS-specific nodes, specify the supported systems:
#     "Operating System :: Microsoft :: Windows",  # Windows specific
#     "Operating System :: POSIX :: Linux",  # Linux specific
#     "Operating System :: MacOS",  # macOS specific
#
#     # GPU Accelerator support. Pick the ones that are supported by your extension.
#     "Environment :: GPU :: NVIDIA CUDA",    # NVIDIA CUDA support
#     "Environment :: GPU :: AMD ROCm",       # AMD ROCm support
#     "Environment :: GPU :: Intel Arc",      # Intel Arc support
#     "Environment :: NPU :: Huawei Ascend",  # Huawei Ascend support
#     "Environment :: GPU :: Apple Metal",    # Apple Metal support
# ]

dependencies = ["# Core ML frameworks", "torch>=2.0.0", "torchvision>=0.15.0", "# SAM3 dependencies (vendored in sam3_lib/)", "timm>=1.0.17", "ftfy==6.1.1", "regex", "iopath>=0.1.10", "huggingface_hub", "# Video support dependencies", "decord>=0.6.0", "opencv-python>=4.8.0", "psutil>=5.9.0", "pycocotools>=2.0.6", "# Standard dependencies", "numpy>=1.26", "Pillow", "tqdm", "typing_extensions"]

[project.urls]
Repository = "https://github.com/PozzettiAndrea/ComfyUI-SAM3"
#  Used by Comfy Registry https://registry.comfy.org
Documentation = "https://github.com/PozzettiAndrea/ComfyUI-SAM3/wiki"
"Bug Tracker" = "https://github.com/PozzettiAndrea/ComfyUI-SAM3/issues"

[tool.comfy]
PublisherId = ""
DisplayName = "ComfyUI-SAM3"
Icon = ""
includes = [] 
# "requires-comfyui" = ">=1.0.0"  # ComfyUI version compatibility

